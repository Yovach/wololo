services:
  wololo-server:
    build:
      context: ./server/
      dockerfile: Dockerfile
    networks:
      - wololo

  wololo-client:
    build:
      context: ./client/
      dockerfile: Dockerfile
      target: production
    networks:
      - wololo

networks:
  wololo: {}

/// List of supported output file extensions for videos
export const SUPPORTED_VIDEO_FORMATS: readonly string[] = Object.freeze(["webm", "mp4", "gif"]);

/// List of supported output file extensions for audios
export const SUPPORTED_AUDIO_FORMATS: readonly string[] = Object.freeze(["mp3", "ogg", "wav"]);

/// List of supported output file extensions for images
export const SUPPORTED_IMAGE_FORMATS: readonly string[] = Object.freeze([
  "png",
  "jpeg",
  "avif",
  "webp",
]);
